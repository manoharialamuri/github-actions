name: triggers demo
on: 
  # if someone opens PR to main branch
  pull_request:
    types: [opened, reopened]
    branches:
      - 'releases/**'
  # push:
  #   branches:
      #- 'feature*'
      #- 'main'
# workflow_dispatch # Triggers the workflow on every push to the repository


jobs:
  # Job name
  trigger:
    runs-on: ubuntu-latest # Github hosted runners
    steps:
      - name: feature branch push
        run: echo "pushing feature branch"
      # - name: manual dispatch
      #   run: echo "Manually Dispatched" # The command to execute